<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <%- include('partials/_header') %>
  <ul>
    <a href="http://localhost:8080/urls/new" ><%="creat a new web ShortURL"%>

    </a>
    <% for (shortURL in urls) {%>
    <li>
      <a href="<%= urls[shortURL]%>" >
        <%="http://local:8080/" + shortURL%>

        </a>
        <%= "( " + urls[shortURL] + " )"%>
        <form method="POST" action="/urls/<%= shortURL %>/delete">
          <input type = "submit" value="delete">
        </form>
        <a href="http://localhost:8080/urls/<%=shortURL%>">
          <input type="submit" value="edite">
        </a>
      </a>
    </li>
    <%}%>
  </ul>
</body>
</html>
