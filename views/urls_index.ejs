<!DOCTYPE html>
<html lang="en">

<head>
  <title>TinyApp</title>
</head>

<body>
  <%- include('partials/_header') %>
    <ul>

      <% for (shortURL in urls) {%>
       <li>
        <a href="<%= urls[shortURL]%>" >
          <%="http://local:8080/" + shortURL%>
        </a>
        <%= urls[shortURL]%>
        </li>
        <%}%>
    </ul>
</body>

</html>
